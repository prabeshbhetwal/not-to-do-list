{
  "name": "api-nottodo",
  "version": "1.0.0",
  "type": "module",
  "description": "This api is create from react not to do frontend application, here is the repo for the frontend `...`",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}
